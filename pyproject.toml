[project]
name = "asteroids"
version = "0.1.0"
description = "Classic Asteroids in Python and Pygame, playable in the browser."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
